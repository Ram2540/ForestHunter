export enum Screens {
    hero,
    mercenaries,
    guild,
    quests,
    ratings
}
// var screens = [];

// export function getArrayOfScreens(): string[]
// {
//     if (screens.length===0)
//     {
//         let keys = Object.keys(Screens);
//         screens = this.keys.slice(this.keys.length / 2);
//     }
//         return screens;
// }
